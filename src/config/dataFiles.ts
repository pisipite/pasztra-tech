// Public JSON contracts consumed by the browser. Keep filenames here so a
// renamed generated file does not require edits across the application shell.
export const DATA_FILES = Object.freeze({
  climateHistory: "govee-history.json",
  bulgariaEnergyMix: "bulgaria-energy-mix.json",
  energyNews: "energy-news.json",
});
